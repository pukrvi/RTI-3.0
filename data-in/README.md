# data-in

Drop a locally saved copy of the official public-authority list here as
`allpa.html`, then run `node scripts/import-authorities.mjs`.

Nothing in this repository fetches that page. Saving it is a thing a person does
in a browser, once; the codebase never touches the live portal.

This directory is gitignored apart from this file.
